server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/epaitoo/spring-cloud-config-server
          clone-on-start: true
          default-label: main

